runtime: python311
service: tiktok-downloader
instance_class: F4_1G

inbound_services:
  - warmup

automatic_scaling:
  max_instances: 2
  max_concurrent_requests: 40

entrypoint: gunicorn -b :$PORT tiktok_downloader.server:app
